meta {
  name: Revoke Session
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/api/auth/revoke-session
  body: json
  auth: inherit
}

body:json {
  {
    "token": "<paste-session-token-here>"
  }
}
