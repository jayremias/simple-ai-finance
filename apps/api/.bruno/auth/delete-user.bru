meta {
  name: Delete User
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/api/auth/delete-user
  body: json
  auth: inherit
}

body:json {
  {
    "callbackURL": "http://localhost:3000/account-deleted"
  }
}
